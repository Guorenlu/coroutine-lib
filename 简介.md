# 项目结构概述

该项目是一个协程库，包含多个模块，主要功能是实现协程、调度器、线程池、定时器以及基于 epoll 和 libevent 的网络编程。  
以下是项目的主要部分及其功能：

---

## 1. 协程模块 (fiber)

- **文件路径**：`fiber_lib/2fiber`
- **功能**：实现协程的创建、切换和销毁。协程是项目的核心，提供了异步编程的基础。
- **推荐阅读顺序**：
  - `fiber.h`：定义协程类及其接口。
  - `fiber.cpp`：实现协程的具体逻辑。
  - `test.cpp`：测试协程的基本功能。

---

## 2. 调度器模块 (scheduler)

- **文件路径**：`fiber_lib/3scheduler`
- **功能**：实现协程的调度，结合线程池和任务队列，支持多线程协程调度。
- **推荐阅读顺序**：
  - `scheduler.h`：定义调度器类及其接口。
  - `scheduler.cpp`：实现调度器的具体逻辑。
  - `main.cpp`：测试调度器的功能。

---

## 3. 线程模块 (thread)

- **文件路径**：`fiber_lib/1thread`
- **功能**：实现线程的封装，提供线程同步和线程池管理功能。
- **推荐阅读顺序**：
  - `thread.h`：定义线程类及其接口。
  - `thread.cpp`：实现线程的具体逻辑。
  - `read_me.txt`：说明如何测试线程模块。

---

## 4. I/O 管理模块 (iomanager)

- **文件路径**：`fiber_lib/5iomanager`
- **功能**：基于 epoll 实现事件驱动的 I/O 管理，支持异步 I/O 操作。
- **推荐阅读顺序**：
  - `fiber.h` 和 `fiber.cpp`：协程的基础。
  - `scheduler.h` 和 `scheduler.cpp`：调度器的基础。
  - `main.cpp`：测试 I/O 管理功能。

---

## 5. HOOK 模块 (hook)

- **文件路径**：`fiber_lib/6hook`
- **功能**：通过 HOOK 技术将同步 I/O 函数转化为异步版本，提升性能。
- **推荐阅读顺序**：
  - `hook.h`：定义 HOOK 的接口。
  - `thread.h` 和 `scheduler.h`：协程和调度器的基础。
  - `main.cpp`：测试 HOOK 功能。

---

## 6. 基于 libevent 的网络模块

- **文件路径**：`fiber_lib/libevent`
- **功能**：基于 libevent 实现事件驱动的网络编程，支持高并发 HTTP 服务器。
- **推荐阅读顺序**：
  - `main.cpp`：实现 HTTP 服务器。
  - `CMakeLists.txt`：编译配置。

---

## 7. 基于 epoll 的网络模块

- **文件路径**：`fiber_lib/epoll`
- **功能**：基于 epoll 实现事件驱动的网络编程，支持高并发 HTTP 服务器。
- **推荐阅读顺序**：
  - `main.cpp`：实现 HTTP 服务器。
  - `CMakeLists.txt`：编译配置。

---

# 各模块之间的关系

- 协程模块是项目的核心，其他模块都依赖于协程的功能。
- 调度器模块负责管理协程的运行，结合线程模块实现多线程调度。
- 线程模块提供线程池和线程同步功能，为调度器和 I/O 管理模块提供支持。
- I/O 管理模块基于协程和调度器，实现异步 I/O 操作。
- HOOK 模块通过 HOOK 技术增强 I/O 管理模块的功能。
- 网络模块（libevent 和 epoll）基于协程和 I/O 管理模块，实现高并发网络服务器。

---

# 推荐阅读顺序

1. 从 **协程模块** 开始，理解协程的基本概念和实现。  
2. 阅读 **线程模块**，了解线程的封装和同步机制。  
3. 阅读 **调度器模块**，理解协程的调度逻辑。  
4. 阅读 **I/O 管理模块**，了解事件驱动的异步 I/O 操作。  
5. 阅读 **HOOK 模块**，理解 HOOK 技术的实现。  
6. 最后阅读 **网络模块**，结合前面模块的功能实现高并发网络服务器。



